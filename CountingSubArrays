#include <bits/stdc++.h>
using namespace std;

int countAllSubstrings(string &s) {
    int res = 0;

    for (int j = 0; j < s.size(); j++) {
        res += (j + 1);
    }

    return res;
}

int main() {
	
    string s;
    cin>>s;
    cout<<countAllSubstrings(s);
}
